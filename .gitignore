# Node
node_modules/
npm-debug.log*

# Playwright
.playwright/
test-results/

# OS
.DS_Store

# Credentials / sessions (never commit)
creds*.json
*cookies*.txt
tools/competitor-analytics/*.png

# GfK / GrooveForce-licensed third-party analytics outputs — deliver directly, never version.
# Analysis reports embed licensed streaming figures, so ignore all report HTML + data exports.
reports/*.html
reports/**/*.json
reports/**/*.csv
qlono_*.json
song_data.json
